# nmm-ota

Over-the-air update manifest and signed release APKs for the New Millennium Android TV app suite.

Boxes fetch `manifest.json` from the `main` branch; APKs are attached as GitHub release assets (not committed to the repo).

